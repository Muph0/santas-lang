

let prepare = a task: N -> N
    contribute n
.

let fill = a task: N -> N
    consider n
    let a = take n - 1
    let b = take n - 2
    contribute a + b
    if n < k: do n + 1.
.

Santa will:
    let fibo = a gift: N -> N.
    prepare fibo(0)
    prepare fibo(1)
    fill(10) fibo(2)
.
